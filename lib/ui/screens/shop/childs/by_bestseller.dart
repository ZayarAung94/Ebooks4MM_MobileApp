import 'package:flutter/material.dart';

class SearchByBestSeller extends StatelessWidget {
  const SearchByBestSeller({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
