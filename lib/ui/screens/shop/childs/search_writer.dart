import 'package:flutter/material.dart';

class SearchByWriterName extends StatelessWidget {
  const SearchByWriterName({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
